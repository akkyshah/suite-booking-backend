import chai from "chai";
import chaiHttp from "chai-http";

chai.use(chaiHttp);

before(async function () {
    // TODO: setup test env
  }
);

// TODO: run tests

after(async () => {
  // TODO: release resources
});
